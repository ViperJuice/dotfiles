guidance:
  level: "minimal"
  # Uncomment and edit custom_instructions to override the default L0 handshake text:
  # custom_instructions: |
  #   Workflow: catalog_search → describe → invoke.
  #   Add your own trigger patterns and guidance here.
  layers:
    mcp_instructions: true
    code_hints: true
    code_snippets: false
    methodology_resource: false
