guidance:
  level: "minimal"
  custom_instructions: |
    Workflow: pmcp_catalog_search -> pmcp_describe -> pmcp_invoke.
    For docs lookup, use Context7 (resolve library ID first, then get docs).
    Need a capability not listed? Use pmcp_request_capability or pmcp_provision.
  layers:
    mcp_instructions: true
    code_hints: true
    code_snippets: false
    methodology_resource: false
